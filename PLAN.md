# 🐧 FLAPPY PENGUIN 3D — 기획서

## 1. 컨셉

웹캠 앞에서 **양팔을 새처럼 움직이면** 화면 속 펭귄이 날아간다.
레트로 3D(로우폴리 + 픽셀레이트 + 플랫셰이딩) 스타일의 무한 비행 게임.

- 입력: 웹캠 → MediaPipe Pose (33 landmark)
- 출력: Three.js 3D 씬 (저해상도 렌더 → 업스케일 = 90년대 콘솔 느낌)
- 목표: 지형에 부딪히지 않고 최대한 멀리 날며 물고기를 먹는다

## 2. 조작 매핑 (요구사항)

| 동작 | 판정 | 결과 |
|---|---|---|
| 양팔 높이 같음 | \|왼팔높이 - 오른팔높이\| < 임계값 | 직진 |
| 오른팔 내림 | 오른팔높이 < 왼팔높이 | 오른쪽 선회 |
| 왼팔 내림 | 왼팔높이 < 오른팔높이 | 왼쪽 선회 |
| 파닥파닥 날개짓 | 양팔 평균 높이의 진동 속도 ↑ | 상승 |
| 날개짓 없음 | 진동 속도 ↓ | 하강(중력) |

### 2.1 신호 처리
정규화된 "팔 높이"를 사용해 카메라 거리/체격에 무관하게 만든다.

```
sw          = 어깨 사이 거리 (픽셀)              # 신체 스케일
raiseL      = (어깨L.y - 손목L.y) / sw           # 위로 들면 +
raiseR      = (어깨R.y - 손목R.y) / sw
roll        = raiseL - raiseR                    # + 면 오른팔이 낮음 → 우회전
avg         = (raiseL + raiseR) / 2              # 날개짓 감지용
flapPower   = EMA(|d(avg)/dt|)                   # 진동의 세기
```

- `roll`: 데드존(0.12) 적용 후 ±1로 정규화 → 선회 입력
- `flapPower`: 1.3~3.6 구간을 0~1로 매핑 → 양력 (실사용 웹캠 기준으로 조정)
- 모든 신호는 지수이동평균(EMA)으로 스무딩하여 랜드마크 지터 제거
- 랜드마크 visibility < 0.5 이면 입력 무시(직전 값 감쇠)

## 3. 물리

```
yaw    += -roll * TURN_RATE * dt          # 선회
speed   = BASE_SPEED + 강하 보너스(-vy에 비례)
vy     += (flapPower * LIFT - GRAVITY) * dt
vy     *= drag
pos    += forward(yaw) * speed * dt + (0, vy, 0) * dt
```

- 지면 높이 + 2.5 아래로 내려가면 **충돌** → 화면 플래시, 튕겨오름, 콤보 리셋
- 최고 고도 제한(160) — 그 위는 구름층

## 4. 월드 (무한 생성)

| 요소 | 구현 |
|---|---|
| 지형 | PlaneGeometry(400, 96×96)를 플레이어 기준으로 재중심화. 높이는 결정론적 sin 합성 노이즈. 고도별 정점 컬러(눈 / 바위 / 얼음) |
| 나무 | 원뿔 3단 + 원기둥 줄기, 눈 덮인 침엽수. 오브젝트 풀 200개 재활용 |
| 돌 | Icosahedron 저폴리 + 랜덤 스케일 |
| 구름 | 구 3~5개 클러스터, 고고도에 배치, 반투명 |
| 물고기 | 회전하는 수집 아이템, 반경 4 이내 접근 시 획득 |

**재활용 방식**: 플레이어에서 반경 R_far(190) 밖으로 나간 오브젝트는
진행 방향 전방 부채꼴(±70°, 거리 90~180) 안으로 재배치.
지형 높이 함수로 y를 결정하므로 지면에 정확히 붙는다.

## 5. 레트로 3D 연출

- 렌더 해상도 1/3.5 축소 → CSS `image-rendering: pixelated` 업스케일
- 모든 머티리얼 `flatShading: true` (그라디언트 없는 각진 면)
- 라이트 2개만: 반구광 + 방향광 (PBR 아님, Lambert)
- 짙은 안개(fog)로 드로우 디스턴스를 감추고 지평선 색과 일치
- 제한된 팔레트: 눈 흰색 / 얼음 하늘색 / 침엽수 진녹색 / 노을 주황
- HUD: 모노스페이스 + 스캔라인 오버레이 + 픽셀 테두리

## 6. 화면 구성

```
┌─────────────────────────────────────────┐
│ SPEED 24  ALT 42  DIST 1.2km   FISH 7   │  ← HUD
│                                         │
│              (3D 게임 화면)              │
│                                         │
│ ┌──────────┐                            │
│ │ 웹캠 미리보기 │  ← 스켈레톤 + 입력 게이지 │
│ │  ◄ ▲ ►   │                            │
│ └──────────┘                            │
└─────────────────────────────────────────┘
```

## 7. 파일 구조

```
penguin/
├── index.html      # 부트스트랩, importmap, 오버레이 UI
├── css/style.css   # 레트로 HUD / 스캔라인 / 시작 화면
└── js/
    ├── main.js     # 초기화, 게임 루프, 카메라, 물리
    ├── pose.js     # MediaPipe 래퍼 → {roll, flapPower, tracked}
    ├── penguin.js  # 절차적 펭귄 모델 + 날개짓/뱅킹 애니메이션
    ├── world.js    # 지형/나무/돌/구름/물고기 + 재활용
    └── hud.js      # HUD 갱신, 웹캠 오버레이 렌더
```

## 8. 폴백

웹캠이 없거나 MediaPipe 로드에 실패해도 플레이 가능해야 한다.
→ **키보드 모드**: ←/→ 선회, Space 연타 = 날개짓.
시작 화면에서 두 모드 중 선택.

## 9. 실행

ES 모듈 + 웹캠 권한 때문에 `file://` 로는 동작하지 않는다. 로컬 서버 필요.

```
python -m http.server 8000
→ http://localhost:8000
```
